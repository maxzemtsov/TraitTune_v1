# Dependencies
            /node_modules
            /.pnpm-store

            # Build artifacts
            /.next/
            /out/

            # Local Environment Variables
            .env*.local
            !.env.example

            # Logs
            npm-debug.log*
            yarn-debug.log*
            yarn-error.log*
            ```
        *   **Example for `traittune_backend/.gitignore` (ensure it exists or create it):**
            ```
            # Dependencies
            /node_modules

            # Local Environment Variables
            .env
            !.env.example

            # Logs
            npm-debug.log*
            yarn-debug.log*
            yarn-error.log*
            nohup.out




            # Dependencies
/node_modules

# Environment variables
.env
.env.*
!.env.example

# Log files
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html) 
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Build output
/dist
/build
.DS_Store

# Mock data and assessment JSON data files
/src/data/mock/
# Если у вас есть другие JSON файлы с данными вне /src/data/mock/, которые не должны быть в git,
# добавьте их сюда, например:
/src/data/*.json
# /src/data/segments.json
